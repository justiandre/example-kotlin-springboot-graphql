/**
 * O intuito desse pacote é manter as classes responsáveis pela infraestrutura da aplicação ou classes utilitárias,
 * o fato é que esse pacote não deveria existir, o tal código visto aqui, deveria estar em componentes isolados fora
 * da aplicação, podendo ser reaproveitado também em outras aplicações e fazendo um isolamento do codigo de negócio
 * do codigo de infraestrutura ou utilitários.
 */

package tech.justi.example.kotlin.springboot.graphql.infrastructure;